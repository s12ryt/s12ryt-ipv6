# 專案文件與依賴表

| 路徑 | 用途 | 主要依賴 |
| --- | --- | --- |
| `cmd/s12ryt-ipv6/` | 前景 service、version、安全管理密碼重設、管理埠讀寫，以及供本機 Agent 使用的 strict JSON/YAML CLI；處理 SIGTERM、機器輸出、退出碼、timeout 與 control client | `internal/app`、`internal/admin`、`internal/secret`、`gopkg.in/yaml.v3`、Go 標準庫 |

  - 第十八輪新增：POST /api/operations/restart（Web 重啟服務，202 非同步+audit）、GET /api/logs/stream（SSE 即時日誌，依賴 eventlog.Subscribe 廣播）；OperationsCoordinator 增加 Restart/RestartDelay 選項
| `internal/app/` | Linux production 組裝與生命週期、雙棧管理 listener、健康聚合、動態政策、連通性測試、drain 閉環、流量觀察、service lock及資料路徑 | `internal/admin`、`config`、`dns64`、`eventlog`、`firewall`、`ipv6resource`、`network`、`node`、`policy`、`proxy`、`secret`、`stats`、`webui` |

  - 第二十二至二十四輪修正 watchdog：由 node 宣告經 `InboundConfigResolver` 取得實際 listener；循序綜合探測 9 個雙棧與 3 個 IPv6-only 網域，NAT64 手動配置或自動監視健康時再加入 DNS64 A-only 與 NAT64 IPv4 literal，全部路徑失敗才計入重啟；失敗按 target 隔離、黏著複驗並回收已移除 target 狀態。
| `internal/config/` | 版本化 YAML 設定、預設值、嚴格驗證與原子保存 | `gopkg.in/yaml.v3` |
| `internal/ipv6resource/` | 前綴範本、canonical 地址、固定地址、動態池、刷新與 draining | `net/netip`、`crypto/rand` 相容 reader |
| `internal/policy/` | IPv4/IPv6/ULA/NAT64/本機及管理地址目的政策 | `net/netip` |
| `internal/secret/` | master key、AES-GCM 祕密庫、代理憑證及 Argon2id 管理密碼 | `crypto/*`、`golang.org/x/crypto/argon2` |
| `internal/auth/` | 單一管理 session、CSRF token 與雙層登入限速 | `crypto/rand`、`crypto/sha256`、`net/netip` |
| `internal/eventlog/` | JSONL 事件白名單、祕密去敏、檔案輪替與清除稽核 | Go 標準庫 |
| `internal/cicheck/` | 以結構化 YAML 契約測試固定 GitHub Actions runner、timeout、action SHA、品質／供應鏈閘門與 release 最小權限 | `.github/workflows/`、`gopkg.in/yaml.v3`、`golang.org/x/mod/modfile` |
| `internal/stats/` | 節點即時/累計統計、單節點/全部歸零及原子快照 | Go 標準庫 |
| `internal/dns64/` | IPv6-only DoT、A/AAAA 快取、目的政策、本地 DNS64 合成、RFC 7050 類型探測及健康監視 | `github.com/miekg/dns`、`internal/policy` |
| `internal/network/` | IPv6 address/local route/external 三模式交易、ownership 對帳、DAD、freebind，以及 Linux 介面、全球 IPv6 地址與核心路由候選偵測 | `internal/ipv6resource`、`github.com/vishvananda/netlink`、`golang.org/x/sys/unix` |
| `internal/firewall/` | 自有 nftables table 完整替換、動態開孔、關閉清理及外部 drop 診斷 | `github.com/google/nftables`、`golang.org/x/sys/unix` |
| `internal/proxy/` | IPv6-only 來源池/dialer、TCP/UDP 埠配置、SOCKS5 TCP+UDP、HTTP、mixed 與 system socket adapter | `github.com/things-go/go-socks5`、`internal/dns64`、`internal/policy` |
| `internal/node/` | 節點設定與生命週期、持久資料夾、批次建立交易、資料夾改名／移動、協定 handler 組裝、listener runtime、交易式更新及防火牆開孔協調 | `internal/proxy`、`internal/firewall`、`internal/policy`、`internal/secret` |
| `internal/admin/` | 管理密碼、單一 session HTTP/CSRF/限速、單節點／批次／資料夾／資源／操作 API、登入保護的網路候選與前綴衝突 API、SSE、資源與操作協調器；另提供 4 MiB Unix control socket/client、完整 Agent JSON Schema、遮罩 export、宣告式 apply、命令式操作、依賴預檢與固定錯誤 envelope | `internal/auth`、`config`、`dns64`、`eventlog`、`firewall`、`ipv6resource`、`network`、`node`、`secret`、`stats` |
| `web/` | 繁中 React 管理 SPA：登入、總覽、節點批次預覽與資料夾操作、IPv6 資源、網路、日誌、SSE 增量刷新、基礎／進階模式、自動命名、網路候選與 DNS64 預設、代理 URI 與剪貼簿備援；另含可及性 modal、髒表單巢狀確認、統一表單控制、可收合側欄、原生動畫、響應式介面及根套件 SPDX 授權中繼資料 | React、TypeScript、Vite、Vitest、Testing Library、Lucide、`LICENSE` |
| `web/go.mod` | 阻隔根 Go 模組掃描 `node_modules` 的工具邊界 | Go toolchain |
| `Dockerfile`、`compose.yaml` | Node/Go 多階段 image 與 host-network、NET_ADMIN、持久 volume 範例 | Docker BuildKit、Linux host |
| `install.sh` | GitHub Release 一鍵安裝/升級器：平台偵測、SHA-256、互斥鎖、HTTP與Agent control雙重健康閘門、CLI quickstart、UFW安全開孔與完整回滾 | GitHub Releases、curl、systemd、flock、nftables、`cmd/s12ryt-ipv6` |
| `.goreleaser.yaml` | Linux amd64/arm64裸binary、含授權與部署文件的tar.gz及checksums發布契約 | GoReleaser v2、`web/dist`、`LICENSE` |
| `.github/workflows/release.yml` | `v*` tag與手動版本發布；唯讀verify完成前後端、供應鏈、漏洞及發布設定檢查，僅publish job取得寫入權限並使用已驗證web artifact發布 | GitHub Actions、Node.js、Go 1.25.13、GoReleaser 2.18.1、`internal/cicheck` |
| `.github/workflows/ci.yml` | push/PR/手動觸發：actionlint、PR dependency review、npm audit/lint/test/build、Go module一致性、nested web測試、gofmt/vet/govulncheck/`-race -shuffle`、linux amd64/arm64建置、deploy契約與一次性netns整合；runner、timeout及actions SHA皆受契約測試約束 | GitHub Actions、Node.js、Go 1.25.13、ip netns、`web/embed.go`、`internal/cicheck` |
| `deploy/` | systemd unit、共用根交易核心的offline installer、移除腳本及shell/release／授權契約測試 | `install.sh`、systemd、Linux capabilities、`LICENSE`、npm package metadata |
| `LICENSE` | GNU Affero General Public License v3 標準全文；專案依 README 的聲明採 `AGPL-3.0-or-later` | Free Software Foundation 授權本文、`README.md` |
| `README.md` | 一鍵安裝/升級、Agent CLI、建置、前景執行、systemd、Docker、面板模式、IPv6／代理詞彙、操作對照、網路前置條件、integration 測試及專案授權聲明 | 全專案、`LICENSE` |
| `agent/question.md` | 已確認需求、約束及驗收契約 | 使用者決策 |
| `agent/deep_todos.md` | 跨工作階段完整任務進度 | `agent/question.md` |
| `agent/項目表.md` | 文件/目錄職責及依賴關係 | 專案結構 |
| `agent/memory.md` | 讀寫、建立、驗證與重要決策紀錄 | 全專案 |
